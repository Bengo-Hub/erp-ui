# Flat node_modules so the runtime `node server.js` (Express 5 -> qs -> side-channel)
# can resolve transitive deps. pnpm v10's default isolated linker breaks require() of
# transitive packages at runtime. See feedback_pnpm_frozen_lockfile.
node-linker=hoisted
shamefully-hoist=true