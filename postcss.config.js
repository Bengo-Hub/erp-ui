// Tailwind CSS v4 is handled by the @tailwindcss/vite plugin (see vite.config.mjs),
// which also covers autoprefixing and CSS imports. No PostCSS plugins needed here.
module.exports = {
    plugins: {}
};
